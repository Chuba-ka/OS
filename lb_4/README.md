# Запуск программ

```bash
# Перейти в папку с проектом
cd /home/unix/sem3/OS/lb_4

# Собрать проект
cd build
cmake ..
cmake --build .

# Запустить программу со статической линковкой
./static_app

# Запустить программу с динамической загрузкой (с переключением по 0)
./dynamic_app